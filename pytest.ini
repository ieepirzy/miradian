[pytest]
# vault.py and server.py sit at the repo root, not in a package. Without this,
# a bare `pytest` (as CI runs it) cannot import them -- only `python -m pytest`
# works, because that happens to put the CWD on sys.path.
pythonpath = .
testpaths = tests
